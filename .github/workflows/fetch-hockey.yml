name: 🏒 Fetch Hockey Data

on:
  schedule:
    # Tous les jours à 8h00 UTC (9h France) et 20h00 UTC (21h France)
    - cron: '0 8,20 * * *'
  # Permet de lancer manuellement depuis l'interface GitHub
  workflow_dispatch:

jobs:
  fetch-data:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Télécharger le repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🟢 Configurer Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 📦 Installer les dépendances (si nécessaire)
        run: |
          if [ -f package.json ]; then
            npm install
          fi

      - name: 🏒 Récupérer les données LIIGA et AHL
        run: node data-fetcher.js
        continue-on-error: true

      - name: 💾 Sauvegarder les données dans le repo
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "IceStats Bot"
          git add data/
          git diff --quiet && git diff --staged --quiet || git commit -m "🏒 Mise à jour données $(date '+%Y-%m-%d %H:%M')"
          git push
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
