name: 🏒 Fetch Hockey Data

on:
  schedule:
    # Exécute à 8h et 20h (UTC) = 9h et 21h (GMT+1 France)
    - cron: '0 8,20 * * *'
  workflow_dispatch: # Permet de déclencher manuellement

jobs:
  fetch-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: 📥 Récupérer le code
        uses: actions/checkout@v3

      - name: 🔧 Installer Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: 📍 Récupérer les données
        run: node data-fetcher.js

      - name: 📤 Commit et Push les changements
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add data.json
          git commit -m "🔄 Mise à jour données hockey $(date '+%Y-%m-%d %H:%M:%S')" --allow-empty
          git push
