name: 🏒 Fetch Hockey Data

on:
  schedule:
    - cron: '0 8 * * *'    # 8h UTC = 9h Paris (hiver) / 10h (été)
    - cron: '0 20 * * *'   # 20h UTC = 21h Paris
  workflow_dispatch:        # Lancement manuel depuis Actions

jobs:
  fetch:
    runs-on: ubuntu-latest
    permissions:
      contents: write       # IMPORTANT : autorisation d'écrire dans le repo

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run data fetcher
        run: node data-fetcher.js

      - name: Commit & push JSON files
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/liiga.json data/ahl.json
          git diff --cached --quiet || git commit -m "🏒 Mise à jour données $(date -u '+%Y-%m-%d %H:%M')"
          git push
